@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<h1>Список игр</h1>
<table class="btn btn-group">
    <thead>
        <tr>
            <th>Название</th>
            <th>Цена</th>
            <th>Подробнее</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var game in Model.Games)
        {
            <tr>
                <td>@game.Title</td>
                <td>@game.Price</td>
                <td>
                    <form>
                        <input type="hidden" name="id" value="@game.Id" />
                        <button type="submit" class="btn btn-primary" asp-page-handler="RedirectToMoreAboutGame">Подробнее</button>
                        <button type="submit" class="btn btn-primary" asp-page-handler="DeleteGame">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
